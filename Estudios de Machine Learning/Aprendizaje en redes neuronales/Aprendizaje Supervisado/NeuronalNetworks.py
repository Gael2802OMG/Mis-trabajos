"""
Compuertas XOR: Son dos entradas binarias (0 o 1) y la salida será 1 solo sí
una de las entradas es verdadera (1) y otra falsa (0)

Combinaciones posibles:
XOR(0,0) = 0
XOR(0,1) = 1
XOR(1,0) = 1
XOR(1,1) = 0
"""
#Creamos Redes Neuronales
import numpy as np
from keras.models import Sequential
from keras.layers import Dense

